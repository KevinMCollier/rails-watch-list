<h1><%= @list.name %></h1>

<h2>Movies in this list:</h2>
<ul>
  <% @movies.each do |movie| %>
    <li><%= movie.title %></li>
  <% end %>
</ul>

<h2>Add bookmark</h2>
<%= link_to 'add a movie', new_list_bookmark_path(@list)%>
